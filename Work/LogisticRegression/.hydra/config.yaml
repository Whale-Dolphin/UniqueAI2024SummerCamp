dataset:
  impute:
    _target_: utils.DataFrameImputer
  impute_method: knn_impute
  normalize:
    _target_: utils.DataFrameNormalizer
  normalize_method: min_max_normalization
  path: ../../Dataset/taitanic.csv
model:
  batch_size: 16
train:
  log_dir: logs/
  epochs: 100
  lr: 0.001
